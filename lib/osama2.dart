import 'package:flutter/material.dart';

class osama extends StatelessWidget {
  const osama({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'osama',

        ),
        centerTitle: true,
      ),
    );
  }
}
